# Daylight 个人日程网站 — 部署说明

## 线上地址（已上线 ✅）

**https://8862380.github.io/daylight/**

代码仓库：https://github.com/8862380/daylight

## 一、本地部署（已完成 ✅）

网站已经在本机启动，浏览器打开即可访问：

**http://127.0.0.1:8000**

之后如果电脑重启，手动重新启动的命令：

```powershell
python -m http.server 8000 --bind 127.0.0.1 --directory "C:\Users\yong\Documents\Codex\2026-08-31\ai-ai-ai-image2\outputs"
```

想用手机在同一 Wi-Fi 下访问，可以把 `--bind 127.0.0.1` 换成 `--bind 0.0.0.0`，然后浏览器打开 `http://电脑IP:8000`。

## 二、在线部署（免费，任选其一）

### 方案 A：GitHub Pages（推荐，稳定免费）

1. 打开 [github.com](https://github.com) 注册/登录，新建一个公开仓库，例如 `daylight`。
2. 把 `index.html`、三个 `variant-*.html` 和 `previews/` 文件夹全部上传到仓库。
3. 进入仓库 **Settings → Pages**，Source 选择 `main` 分支、目录 `/ (root)`，保存。
4. 等 1–2 分钟，访问 `https://你的用户名.github.io/daylight/`。

### 方案 B：Netlify Drop（拖拽即可，最快）

1. 打开 [app.netlify.com/drop](https://app.netlify.com/drop)。
2. 把 `daylight-site.zip` 直接拖进页面。
3. 几秒钟后获得 `https://xxxx.netlify.app` 的正式链接。

### 方案 C：Vercel

1. 打开 [vercel.com/new](https://vercel.com/new)，登录。
2. 选择「Deploy without Git」，把 `daylight-site.zip` 或文件夹拖进去。
3. 获得 `https://xxxx.vercel.app` 链接。

## 三、内容怎么改

- 任务、工作、目标、消费、课程、学习时间等示例数据，都在对应的 `variant-*.html` 文件里，直接编辑文字即可。
- 任务勾选状态会保存在浏览器本地（localStorage），刷新不丢；换浏览器或清缓存会恢复默认示例。
- 想改名字「小宇」：搜索 `早上好，小宇` 和页面里的「宇」头像即可替换。

## 四、仅自己用（私有）的部署方案

> 免费托管平台的默认链接都是公开的。想「只有自己能看」，按下面方案二选一。

### 方案 1：Vercel 私有部署（推荐，免费）

1. 打开 [vercel.com/new](https://vercel.com/new)，用 GitHub 账号或邮箱注册登录。
2. 把 `daylight-site.zip` 拖进去部署，获得 `https://xxx.vercel.app`。
3. 打开 **Settings → Deployment Protection**，开启 **Vercel Authentication**：
   - 只勾选「Only Vercel Authentication」（自己登录 Vercel 就能看）；
   - 或选择「Specific Visitors」并把你的邮箱加进白名单。
4. 之后任何人打开链接都要先登录/验证邮箱，别人看不到内容。

### 方案 2：局域网自用（零成本、不花钱、马上能用）

只在自己的设备上使用（同一 Wi-Fi 下的手机/平板/电脑）：

```powershell
python -m http.server 8000 --bind 0.0.0.0 --directory "C:\Users\yong\Documents\Codex\2026-08-31\ai-ai-ai-image2\outputs"
```

然后手机浏览器打开 `http://电脑的局域网IP:8000`（IP 可用 `ipconfig` 查看，通常类似 `192.168.x.x`）。

> 注意：如果局域网 IP 打不开，多半是 Windows 防火墙拦截，需要在防火墙里允许 Python 入站，或先保证两台设备在同一 Wi-Fi。

### 不推荐但也可以了解

- **GitHub Pages**：免费账号的 Pages 站点是公开的；要私有需要 GitHub Pro（付费）。
- **Netlify**：密码保护属于 Pro 功能（付费），免费版只能公开。
- **Cloudflare Tunnel + Access**：可以做到登录邮箱后才能访问，免费，但配置较复杂，适合有 Cloudflare 经验的人。
