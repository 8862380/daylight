# Daylight 个人日程网站 — 部署说明

## 线上地址（已上线 ✅）

**https://8862380.github.io/daylight/**

代码仓库：https://github.com/8862380/daylight

## 一、本地部署（已完成 ✅）

网站已经在本机启动，浏览器打开即可访问：

**http://127.0.0.1:8000**

之后如果电脑重启，手动重新启动的命令：

```powershell
python -m http.server 8000 --bind 127.0.0.1 --directory "C:\Users\yong\Documents\Codex\2026-08-31\ai-ai-ai-image2\outputs"
```

想用手机在同一 Wi-Fi 下访问，可以把 `--bind 127.0.0.1` 换成 `--bind 0.0.0.0`，然后浏览器打开 `http://电脑IP:8000`。

## 二、在线部署（免费，任选其一）

### 方案 A：GitHub Pages（推荐，稳定免费）

1. 打开 [github.com](https://github.com) 注册/登录，新建一个公开仓库，例如 `daylight`。
2. 把 `index.html`、三个 `variant-*.html` 和 `previews/` 文件夹全部上传到仓库。
3. 进入仓库 **Settings → Pages**，Source 选择 `main` 分支、目录 `/ (root)`，保存。
4. 等 1–2 分钟，访问 `https://你的用户名.github.io/daylight/`。

### 方案 B：Netlify Drop（拖拽即可，最快）

1. 打开 [app.netlify.com/drop](https://app.netlify.com/drop)。
2. 把 `daylight-site.zip` 直接拖进页面。
3. 几秒钟后获得 `https://xxxx.netlify.app` 的正式链接。

### 方案 C：Vercel

1. 打开 [vercel.com/new](https://vercel.com/new)，登录。
2. 选择「Deploy without Git」，把 `daylight-site.zip` 或文件夹拖进去。
3. 获得 `https://xxxx.vercel.app` 链接。

## 三、内容怎么改

- 任务、工作、目标、消费、课程、学习时间等示例数据，都在对应的 `variant-*.html` 文件里，直接编辑文字即可。
- 任务勾选状态会保存在浏览器本地（localStorage），刷新不丢；换浏览器或清缓存会恢复默认示例。
- 想改名字「小宇」：搜索 `早上好，小宇` 和页面里的「宇」头像即可替换。

## 四、仅自己用（私有）的部署方案

> 免费托管平台的默认链接都是公开的。想「只有自己能看」，按下面方案二选一。

### 方案 1：Vercel 私有部署（推荐，免费）

1. 打开 [vercel.com/new](https://vercel.com/new)，用 GitHub 账号或邮箱注册登录。
2. 把 `daylight-site.zip` 拖进去部署，获得 `https://xxx.vercel.app`。
3. 打开 **Settings → Deployment Protection**，开启 **Vercel Authentication**：
   - 只勾选「Only Vercel Authentication」（自己登录 Vercel 就能看）；
   - 或选择「Specific Visitors」并把你的邮箱加进白名单。
4. 之后任何人打开链接都要先登录/验证邮箱，别人看不到内容。

### 方案 2：局域网自用（零成本、不花钱、马上能用）

只在自己的设备上使用（同一 Wi-Fi 下的手机/平板/电脑）：

当前已启动：**http://192.168.43.180:8001**（手机连同一个 Wi-Fi 直接打开即可）

以后手动启动的命令：

```powershell
python -m http.server 8001 --bind 0.0.0.0 --directory "C:\Users\yong\Documents\Codex\2026-08-31\ai-ai-ai-image2\outputs"
```

然后手机浏览器打开 `http://电脑的局域网IP:8001`（IP 可用 `ipconfig` 查看）。

> 注意：如果局域网 IP 打不开，多半是 Windows 防火墙拦截，需要在防火墙里允许 Python 入站，或先保证两台设备在同一 Wi-Fi。

### 不推荐但也可以了解

- **GitHub Pages**：免费账号的 Pages 站点是公开的；要私有需要 GitHub Pro（付费）。
- **Netlify**：密码保护属于 Pro 功能（付费），免费版只能公开。
- **Cloudflare Tunnel + Access**：可以做到登录邮箱后才能访问，免费，但配置较复杂，适合有 Cloudflare 经验的人。

## 五、云端多用户（Supabase）接入步骤

目标：数据统一存云端，任何设备登录同一账号看到同一份数据；管理员在后台能看到所有用户的数据。

### 第 1 步：创建 Supabase 项目（免费，约 3 分钟）

1. 打开 [supabase.com](https://supabase.com)，用邮箱或 GitHub 注册登录。
2. 点 **New project**，填项目名称（如 `daylight`）、设数据库密码（记好）、选离你近的地区（如 Singapore），创建。
3. 等 1–2 分钟项目就绪。

### 第 2 步：运行数据库脚本

1. 进入项目 → 左侧 **SQL Editor** → **New query**。
2. 打开同目录的 [supabase-schema.sql](supabase-schema.sql)，**把里面的“你的管理员邮箱@example.com”换成你自己的邮箱**，整段复制粘贴到编辑器，点 **Run**。
3. 显示成功即可。

### 第 3 步：把两个信息发给 Codex

1. 项目 → **Settings → API**（或 **Project Settings → API Keys**）。
2. 复制 **Project URL**（形如 `https://xxxx.supabase.co`）和 **anon / public** 密钥（形如 `eyJ...`）。
3. 发给 Codex（anon 是公开密钥，本身就是给前端用的，安全）。

### 第 4 步：Codex 改造完成

拿到密钥后，Codex 会把登录改成邮箱账号 + 云端同步：

- 注册/登录用邮箱密码，数据存云端
- 任意设备登录同一账号，看到同一份数据
- 管理员（你的邮箱）在后台能看到所有用户的数据
- 本地数据可一键迁移到云端

## 六、云端版使用说明（已上线）

### 日常使用

- 打开 https://8862380.github.io/daylight/ ，**用户名 + 密码**登录（不用邮箱）
- 新用户点“注册新账号”：填用户名（≥2 字符）+ 密码（≥6 位）即可
- 管理员登录：输入框里填邮箱 `2092307480@qq.com` + 密码（系统自动识别邮箱）
- 数据实时云端同步：换手机/电脑登录同一用户名，数据一致

> 重要：用户名注册需要 Supabase 关闭“邮箱确认”：
> 项目 → **Authentication → Providers → Email** → 把 **Confirm email** 开关关掉 → Save

### 管理员（后台看全部用户）

- 管理员邮箱：2092307480@qq.com
- 打开 https://8862380.github.io/daylight/admin.html → 开发者密码（第一次先设置）→ 邮箱登录
- 顶部显示“管理员”标识，“全部用户数据”可查看所有注册用户的数据
- 普通用户只能看/改自己的数据

### 常见问题

- 登录提示 Invalid login credentials：邮箱未确认或密码错误
- 后台没有“管理员”标识：补丁 SQL（is_admin）没跑成功，找 Codex 重新给一条
- 忘记开发者密码：清浏览器站点数据后重新设置
